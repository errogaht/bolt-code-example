<!DOCTYPE html>
<html class="no-js" lang="{{ htmllang() }}">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="google-site-verification" content="ZvijP2ajbo_lPRV84sg70EaG0zt0QhWBYxjq8c9dnLc">
    <link rel="shortcut icon" href="favicon.png">
    {% if record.title is defined %}<title>{{ record.title|striptags }}</title>{% endif %}
    {% if record.description is defined %}<meta name="description" content="{{ record.description|striptags }}">{% endif %}
    {% if record.keywords is defined %}<meta name="keywords" content="{{ record.keywords|striptags }}">{% endif %}

    <link rel="stylesheet" href="{{ paths.theme }}css/main.css">
    <link rel="stylesheet" href="{{ paths.theme }}css/custom.css">
    {#<!-- A variable from the theme's config: {{ theme.foo }} -->#}

    <!-- Yandex.Metrika counter -->
    <script src="https://mc.yandex.ru/metrika/watch.js" type="text/javascript"></script>
    <script type="text/javascript">
        try {
            var yaCounter31836311 = new Ya.Metrika({
                id:31836311,
                clickmap:true,
                trackLinks:true,
                accurateTrackBounce:true,
                webvisor:true
            });
        } catch(e) { }
    </script>
    <noscript><div><img src="https://mc.yandex.ru/watch/31836311" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
    <!-- /Yandex.Metrika counter -->

</head>
<body>

<div class="page">
    <header class="header">
        {# Include the menu, as defined in config/menu.yml. Note that this theme comes with it's own _sub_menu.twig. If this file wasn't included in the current theme, the default menu template in app/theme_defaults/_sub_menu.twig would be used. #}
        {{ menu('main', '_sub_menu.twig') }}

        <div class="logo"><img src="{{ paths.theme }}img/logo.svg" alt=""></div>
    </header>