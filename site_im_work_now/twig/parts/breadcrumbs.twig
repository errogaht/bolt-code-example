{% set parents = breadcrumb() %}
<ol class="breadcrumb">
    {% for item in parents %}
        {% if loop.last %}
            <li class="active">{{ item.title }}</li>
        {% else %}
            <li><a href="{{ item.link }}">{{ item.title }}</a></li>
        {% endif %}
    {% endfor %}
</ol>